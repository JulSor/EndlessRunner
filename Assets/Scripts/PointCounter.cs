using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PointCounter : MonoBehaviour
{

    /*public Text points;
    public int pumpkinPoints;
    // Start is called before the first frame update
    void Start()
    {
        SetCountText();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void SetCountText()
    {
        points.text = pumpkinPoints.ToString();
    }

    public void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag ("pumpkin"))
        {
            Debug.Log("pumpkin hit through pointcounter.cs");
            other.gameObject.transform.localScale = new Vector3(0, 0, 0);
            pumpkinPoints = pumpkinPoints + 1;
            SetCountText();
        }
    }*/
}
